import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'photo-gallery-with-capacitor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
